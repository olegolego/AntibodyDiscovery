id: echo
name: Echo (test)
version: "1.0.0"
category: debug
icon: ""
description: Passes all inputs through as outputs unchanged. Used for pipeline testing.

inputs:
  - name: data
    type: json
    required: true

outputs:
  - name: data
    type: json

runtime:
  kind: local_python
  endpoint_env: ""
  gpu: false
  timeout_seconds: 10
